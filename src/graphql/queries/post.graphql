query Post($id: Int!) {
  post(id: $id) {
    id
    createdAt
    updatedAt
    title
    text
    points
    voteStatus
    creatorId
    creator {
      id
      username
      email
      createdAt
      updatedAt
    }
  }
}
